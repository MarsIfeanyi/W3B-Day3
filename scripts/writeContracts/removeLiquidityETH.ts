import { ethers } from "hardhat";
import { UNISWAPv2_CONTRACT_ADDRESS } from "../contractAddress/address";

async function main() {}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
